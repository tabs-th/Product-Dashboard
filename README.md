# Product-Dashboard
The dashboard will fetch and display product data from an external API. The dashboard can also enhance the UI with interactive hover effects for product cards.
Please run using a local server (like Live Server) to avoid CORS issues caused by the file:// protocol.
